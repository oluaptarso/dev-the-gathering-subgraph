enum CardRarity {
  COMMON
  UNCOMMON
  RARE
  EPIC
  LEGENDARY
}

type Card @entity {
  id: ID!
  externalId: BigInt!
  owner: Bytes!
  rarity: CardRarity!
  foil: Boolean!
  quantity: BigInt!
  level: BigInt!
  created: Boolean!
  createdAt: BigInt!
  updatedAt: BigInt!
}